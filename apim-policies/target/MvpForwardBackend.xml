<!-- This file is generated by the Azure API Management Policy Toolkit -->
<!-- Version: 1.0.0.0 -->
<policies>
  <inbound>
    <base />
    <set-backend-service backend-id="apim-rt-pool" />
    <choose>
      <when condition="@(context.Request.Method.Equals("GET"))">
        <cache-lookup-value key="@(Regex.Match(context.Request.OriginalUrl.ToString(), @"/job/(\d+)").Groups[1].Value)" variable-name="cachedResponse" caching-type="internal" />
        <choose>
          <when condition="@(context.Variables.ContainsKey("cachedResponse"))">
            <set-backend-service base-url="@((string)context.Variables["cachedResponse"])" />
          </when>
        </choose>
      </when>
    </choose>
  </inbound>
  <backend>
    <forward-request />
  </backend>
  <outbound>
    <base />
    <cache-store-value key="@(Regex.Match(context.Response.Headers.GetValueOrDefault("operation-location", ""), @"/job/(\d+)", RegexOptions.IgnoreCase).Groups[1].Value)" value="@(context.Response.Headers.GetValueOrDefault("operation-location", ""))" duration="600" caching-type="internal" />
    <set-header name="operation-location" exists-action="override">
      <value>@(string.Concat("https://policy-testing.azure-api.net/api/v1", Regex.Match(context.Response.Headers.GetValueOrDefault("operation-location", ""), @"/job/\d+").Value))</value>
    </set-header>
  </outbound>
  <on-error>
    <base />
    <set-header name="X-Error" exists-action="override">
      <value>An error occurred in the API pipeline.</value>
    </set-header>
  </on-error>
</policies>